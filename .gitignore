node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.claude/

# Pre-React-migration vanilla HTML/JS and superseded React components.
# Kept on disk for review but untracked.
_archive/

# Internal integration notes and pitch deck — kept locally, not in VCS.
BACKEND_INTEGRATION.md
ROADMAP.md
*.pptx


# Leftover npm-install artifacts from latest-main checkouts (no source here on this branch)
/backend/
/frontend/
